# GraphViz Sample

## Usage

1. Use Python version 3.10.11
2. Run `pip install -r requirements.txt`
3. Run `python main.py`
4. Enjoy

Within `main.py`, add the desired MTX file and change the constant MTX_FILE_NAME to the name of the added MTX file.
